---
import BaseLayout from "../layouts/BaseLayout.astro";
import CallToAction from "../components/CallToAction.astro";
import PageHero from "../components/PageHero.astro";
import ReviewCard from "../components/ReviewCard.astro";
import SectionHeading from "../components/SectionHeading.astro";
import {
  allReviews,
  business,
  createBreadcrumbSchema,
  createWebPageSchema,
} from "../data/site";

const title = "Wesley Chapel A/C Company Reviews | What Homeowners Say";
const description =
  "Read what local homeowners say about working with me on practical repairs and straightforward service.";

const schema = [
  createWebPageSchema({
    pathname: "/reviews/",
    title,
    description,
  }),
  createBreadcrumbSchema([
    { name: "Home", pathname: "/" },
    { name: "Reviews", pathname: "/reviews/" },
  ]),
];
---

<BaseLayout
  title={title}
  description={description}
  pathname="/reviews/"
  image="/images/mike-ac-replacement.jpg"
  schema={schema}
>
  <PageHero
    eyebrow="Proof after the work is done"
    title="What Wesley Chapel Homeowners Say"
    summary="I would rather be known by the experience customers have after the job is done than by any slogan. These reviews give you a sense of the responsiveness, practical repairs, and clear communication I try to provide."
    imageSrc="/images/mike-ac-replacement.jpg"
    imageAlt="Mike standing beside a newly installed air conditioning unit"
    actionPrimary={{ href: business.phoneHref, label: "Call Now" }}
    actionSecondary={{ href: "/contact/", label: "Contact Info" }}
    breadcrumbs={[
      { href: "/", label: "Home" },
      { href: "/reviews/", label: "Reviews" },
    ]}
    highlights={[
      "Clear diagnosis",
      "No unnecessary replacement pressure",
      "Fast, fair, and straightforward service",
    ]}
  />

  <section class="section">
    <div class="container">
      <SectionHeading
        eyebrow="Review themes"
        title="What people mention most"
        intro="These are the things homeowners mention again and again after I finish a good service call."
      />

      <ul class="pill-list">
        <li>Clear diagnosis</li>
        <li>No unnecessary replacement pressure</li>
        <li>Fast response when the AC is down</li>
        <li>Fair, straightforward pricing</li>
        <li>Clear explanations</li>
      </ul>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="review-grid">
        {allReviews.map((review) => (
          <ReviewCard {...review} />
        ))}
      </div>
    </div>
  </section>

  <CallToAction
    title="If you need help with your system, call me."
    body="Call if you want a practical diagnosis or if you want to reach me directly."
    primaryHref={business.phoneHref}
    primaryLabel="Call Now"
    secondaryHref="/contact/"
    secondaryLabel="Contact Info"
  />
</BaseLayout>
